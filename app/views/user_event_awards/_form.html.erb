<%= form_with(model: user_event_award) do |form| %>
  <% if user_event_award.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_event_award.errors.count, "error") %> prohibited this user_event_award from being saved:</h2>

      <ul>
        <% user_event_award.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.collection_select :user_id, User.order(:name), :id, :name, prompt: "Selecione Usuario" %>
  </div>
  <br>
  <div>
    <%= form.label :event_id, style: "display: block" %>
    <%= form.collection_select :event_id, Event.order(:description), :id, :description, prompt: "Selecione Evento" %>
  </div>
  <br>
  <div>
    <%= form.label :award_id, style: "display: block" %>
    <%= form.collection_select :award_id, Event.order(:description), :id, :description, prompt: "Selecione Premio" %>
  </div>
<br>
  <div>
    <%= form.submit %>
  </div>
<% end %>
